/* 🎨 SELINAY 3D NAVIGATION EFFECTS - Premium UI/UX */
/* Advanced 3D Navigation System with Hardware Acceleration */
/* Created: June 9, 2025 - TASK S-1 Implementation */

/* ===== GPU ACCELERATION BASE ===== */
.selinay-3d-nav {
  transform-style: preserve-3d;
  backface-visibility: hidden;
  will-change: transform;
  -webkit-transform-style: preserve-3d;
  -webkit-backface-visibility: hidden;
  -webkit-will-change: transform;
}

/* ===== 3D NAVIGATION CONTAINER ===== */
.selinay-nav-3d {
  position: relative;
  perspective: 1000px;
  -webkit-perspective: 1000px;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
}

/* ===== 3D NAVIGATION ITEMS ===== */
.selinay-nav-item-3d {
  position: relative;
  display: inline-block;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: transform;
  -webkit-will-change: transform;
}

/* ===== HOVER EFFECTS - 3D LIFT ===== */
.selinay-nav-item-3d:hover {
  transform: translateZ(20px) rotateX(5deg) rotateY(-5deg);
  -webkit-transform: translateZ(20px) rotateX(5deg) rotateY(-5deg);
  box-shadow: 
    0 20px 40px rgba(0, 0, 0, 0.15),
    0 10px 20px rgba(0, 0, 0, 0.1),
    0 0 0 1px rgba(255, 255, 255, 0.1);
}

/* ===== ACTIVE STATE - 3D PRESS ===== */
.selinay-nav-item-3d:active {
  transform: translateZ(5px) rotateX(2deg) rotateY(-2deg) scale(0.98);
  -webkit-transform: translateZ(5px) rotateX(2deg) rotateY(-2deg) scale(0.98);
  transition: all 0.1s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.1s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ===== 3D CARD NAVIGATION ===== */
.selinay-card-3d {
  position: relative;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: transform;
  -webkit-will-change: transform;
  border-radius: 12px;
  overflow: hidden;
}

.selinay-card-3d::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.1) 0%, 
    rgba(255, 255, 255, 0.05) 50%, 
    rgba(0, 0, 0, 0.05) 100%);
  opacity: 0;
  transition: opacity 0.3s ease;
  -webkit-transition: opacity 0.3s ease;
  pointer-events: none;
  z-index: 1;
}

.selinay-card-3d:hover::before {
  opacity: 1;
}

.selinay-card-3d:hover {
  transform: translateY(-10px) rotateX(5deg) rotateY(5deg);
  -webkit-transform: translateY(-10px) rotateX(5deg) rotateY(5deg);
  box-shadow: 
    0 25px 50px rgba(0, 0, 0, 0.2),
    0 15px 30px rgba(0, 0, 0, 0.15),
    0 5px 15px rgba(0, 0, 0, 0.1);
}

/* ===== 3D BUTTON SYSTEM ===== */
.selinay-btn-3d {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 12px 24px;
  border: none;
  border-radius: 8px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-weight: 600;
  text-decoration: none;
  cursor: pointer;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: transform;
  -webkit-will-change: transform;
  min-height: 44px;
  min-width: 44px;
  touch-action: manipulation;
}

.selinay-btn-3d::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.2) 0%, 
    rgba(255, 255, 255, 0.1) 50%, 
    rgba(0, 0, 0, 0.1) 100%);
  border-radius: 8px;
  opacity: 0;
  transition: opacity 0.3s ease;
  -webkit-transition: opacity 0.3s ease;
  pointer-events: none;
}

.selinay-btn-3d:hover::before {
  opacity: 1;
}

.selinay-btn-3d:hover {
  transform: translateY(-3px) rotateX(10deg);
  -webkit-transform: translateY(-3px) rotateX(10deg);
  box-shadow: 
    0 15px 30px rgba(102, 126, 234, 0.4),
    0 8px 16px rgba(102, 126, 234, 0.3),
    0 4px 8px rgba(102, 126, 234, 0.2);
}

.selinay-btn-3d:active {
  transform: translateY(-1px) rotateX(5deg) scale(0.98);
  -webkit-transform: translateY(-1px) rotateX(5deg) scale(0.98);
  transition: all 0.1s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.1s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ===== 3D SIDEBAR NAVIGATION ===== */
.selinay-sidebar-3d {
  position: relative;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  perspective: 800px;
  -webkit-perspective: 800px;
}

.selinay-sidebar-item-3d {
  position: relative;
  display: block;
  padding: 12px 20px;
  margin: 4px 0;
  border-radius: 8px;
  text-decoration: none;
  color: #333;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: transform;
  -webkit-will-change: transform;
  overflow: hidden;
}

.selinay-sidebar-item-3d::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, 
    transparent 0%, 
    rgba(255, 255, 255, 0.4) 50%, 
    transparent 100%);
  transition: left 0.5s ease;
  -webkit-transition: left 0.5s ease;
}

.selinay-sidebar-item-3d:hover::before {
  left: 100%;
}

.selinay-sidebar-item-3d:hover {
  transform: translateX(10px) rotateY(-5deg);
  -webkit-transform: translateX(10px) rotateY(-5deg);
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  box-shadow: 
    0 8px 16px rgba(0, 0, 0, 0.1),
    0 4px 8px rgba(0, 0, 0, 0.05);
}

/* ===== 3D DROPDOWN MENU ===== */
.selinay-dropdown-3d {
  position: relative;
  display: inline-block;
  perspective: 1000px;
  -webkit-perspective: 1000px;
}

.selinay-dropdown-menu-3d {
  position: absolute;
  top: 100%;
  left: 0;
  min-width: 200px;
  background: white;
  border-radius: 8px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  opacity: 0;
  visibility: hidden;
  transform: rotateX(-90deg) translateY(-10px);
  -webkit-transform: rotateX(-90deg) translateY(-10px);
  transform-origin: top center;
  -webkit-transform-origin: top center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: transform, opacity;
  -webkit-will-change: transform, opacity;
  z-index: 1000;
}

.selinay-dropdown-3d:hover .selinay-dropdown-menu-3d {
  opacity: 1;
  visibility: visible;
  transform: rotateX(0deg) translateY(0px);
  -webkit-transform: rotateX(0deg) translateY(0px);
}

.selinay-dropdown-item-3d {
  display: block;
  padding: 12px 16px;
  text-decoration: none;
  color: #333;
  transition: all 0.2s ease;
  -webkit-transition: all 0.2s ease;
  border-radius: 4px;
  margin: 4px;
}

.selinay-dropdown-item-3d:hover {
  background: #f8f9fa;
  transform: translateX(5px);
  -webkit-transform: translateX(5px);
}

/* ===== 3D TAB NAVIGATION ===== */
.selinay-tabs-3d {
  display: flex;
  position: relative;
  perspective: 800px;
  -webkit-perspective: 800px;
  border-bottom: 2px solid #e9ecef;
}

.selinay-tab-3d {
  position: relative;
  padding: 12px 24px;
  background: #f8f9fa;
  border: none;
  border-radius: 8px 8px 0 0;
  cursor: pointer;
  transform-style: preserve-3d;
  -webkit-transform-style: preserve-3d;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: transform;
  -webkit-will-change: transform;
  margin-right: 4px;
}

.selinay-tab-3d:hover {
  transform: translateY(-3px) rotateX(10deg);
  -webkit-transform: translateY(-3px) rotateX(10deg);
  background: #e9ecef;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
}

.selinay-tab-3d.active {
  background: white;
  transform: translateY(-5px) rotateX(15deg);
  -webkit-transform: translateY(-5px) rotateX(15deg);
  box-shadow: 
    0 12px 24px rgba(0, 0, 0, 0.15),
    0 6px 12px rgba(0, 0, 0, 0.1);
  z-index: 10;
}

/* ===== PERFORMANCE OPTIMIZATIONS ===== */
@media (prefers-reduced-motion: reduce) {
  .selinay-nav-item-3d,
  .selinay-card-3d,
  .selinay-btn-3d,
  .selinay-sidebar-item-3d,
  .selinay-dropdown-menu-3d,
  .selinay-tab-3d {
    transition: none;
    -webkit-transition: none;
    transform: none !important;
    -webkit-transform: none !important;
  }
}

/* ===== MOBILE OPTIMIZATIONS ===== */
@media (max-width: 768px) {
  .selinay-nav-item-3d:hover,
  .selinay-card-3d:hover,
  .selinay-btn-3d:hover {
    transform: none;
    -webkit-transform: none;
  }
  
  .selinay-nav-item-3d:active,
  .selinay-card-3d:active,
  .selinay-btn-3d:active {
    transform: scale(0.98);
    -webkit-transform: scale(0.98);
  }
  
  .selinay-sidebar-item-3d:hover {
    transform: translateX(5px);
    -webkit-transform: translateX(5px);
  }
}

/* ===== HIGH DPI DISPLAYS ===== */
@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
  .selinay-nav-item-3d,
  .selinay-card-3d,
  .selinay-btn-3d {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
}

/* ===== DARK MODE SUPPORT ===== */
@media (prefers-color-scheme: dark) {
  .selinay-sidebar-item-3d {
    color: #e9ecef;
  }
  
  .selinay-sidebar-item-3d:hover {
    background: linear-gradient(135deg, #343a40 0%, #495057 100%);
  }
  
  .selinay-dropdown-menu-3d {
    background: #343a40;
    color: #e9ecef;
  }
  
  .selinay-dropdown-item-3d {
    color: #e9ecef;
  }
  
  .selinay-dropdown-item-3d:hover {
    background: #495057;
  }
  
  .selinay-tab-3d {
    background: #343a40;
    color: #e9ecef;
  }
  
  .selinay-tab-3d:hover {
    background: #495057;
  }
  
  .selinay-tab-3d.active {
    background: #212529;
  }
}

/* ===== ACCESSIBILITY ENHANCEMENTS ===== */
.selinay-nav-item-3d:focus,
.selinay-btn-3d:focus,
.selinay-sidebar-item-3d:focus,
.selinay-dropdown-item-3d:focus,
.selinay-tab-3d:focus {
  outline: 3px solid #3b82f6;
  outline-offset: 2px;
  z-index: 100;
}

/* ===== ANIMATION PERFORMANCE MONITORING ===== */
.selinay-3d-performance-monitor {
  position: fixed;
  top: 10px;
  right: 10px;
  background: rgba(0, 0, 0, 0.8);
  color: white;
  padding: 8px 12px;
  border-radius: 4px;
  font-size: 12px;
  font-family: monospace;
  z-index: 9999;
  display: none;
}

.selinay-3d-performance-monitor.show {
  display: block;
}

/* ===== SUCCESS INDICATORS ===== */
.selinay-3d-success {
  animation: selinaySuccess 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  -webkit-animation: selinaySuccess 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes selinaySuccess {
  0% { transform: scale(1) rotateZ(0deg); }
  25% { transform: scale(1.05) rotateZ(2deg); }
  50% { transform: scale(1.1) rotateZ(-2deg); }
  75% { transform: scale(1.05) rotateZ(1deg); }
  100% { transform: scale(1) rotateZ(0deg); }
}

@-webkit-keyframes selinaySuccess {
  0% { -webkit-transform: scale(1) rotateZ(0deg); }
  25% { -webkit-transform: scale(1.05) rotateZ(2deg); }
  50% { -webkit-transform: scale(1.1) rotateZ(-2deg); }
  75% { -webkit-transform: scale(1.05) rotateZ(1deg); }
  100% { -webkit-transform: scale(1) rotateZ(0deg); }
} 