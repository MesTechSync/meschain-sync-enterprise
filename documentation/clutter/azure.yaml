# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: meschain-sync-enterprise
metadata:
  template: meschain-sync-enterprise@0.0.1-beta

infra:
  provider: bicep
  path: infra

services:
  functions:
    project: azure-functions
    language: ts
    host: function
